# SAHAYATA 2.0 - ARCHITECTURE & DIAGRAMS

## Visual System Documentation

---

## 📐 SYSTEM ARCHITECTURE

### High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT TIER                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   Browser    │  │  React App   │  │ LocalStorage │         │
│  │  (Chrome/    │→ │  (TypeScript)│→ │  (Forum,     │         │
│  │   Safari)    │  │   + Vite     │  │  Training)   │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│         ↓                 ↓                                      │
│    HTTP/HTTPS        WebSocket                                  │
│         ↓                 ↓                                      │
└─────────────────────────────────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────────┐
│                    APPLICATION TIER                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────┐           │
│  │         Express Server (Node.js + TypeScript)    │           │
│  │                                                   │           │
│  │  ┌─────────┐  ┌─────────┐  ┌──────────────┐    │           │
│  │  │  REST   │  │ Socket  │  │  Middleware  │    │           │
│  │  │   API   │  │   .IO   │  │  (Auth JWT)  │    │           │
│  │  └─────────┘  └─────────┘  └──────────────┘    │           │
│  │       ↓             ↓              ↓             │           │
│  │  ┌─────────────────────────────────────────┐   │           │
│  │  │         Business Logic Layer            │   │           │
│  │  │    (Routes, Controllers, Services)      │   │           │
│  │  └─────────────────────────────────────────┘   │           │
│  └──────────────────────────────────────────────────┘           │
│         ↓                ↓               ↓                       │
└─────────────────────────────────────────────────────────────────┘
         ↓                ↓               ↓
┌─────────────────────────────────────────────────────────────────┐
│                      DATA & SERVICES TIER                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │ MongoDB  │  │  Local   │  │  Twilio  │  │Nodemailer│       │
│  │(Database)│  │   File   │  │  (SMS)   │  │ (Email)  │       │
│  │          │  │  System  │  │          │  │          │       │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘       │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    BACKGROUND PROCESSES                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────┐           │
│  │         Node-Cron (Scheduler)                    │           │
│  │  Every 5 minutes:                                │           │
│  │  1. Check appointments/routines                  │           │
│  │  2. Send notifications (SMS/Email/In-app)       │           │
│  └──────────────────────────────────────────────────┘           │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔐 AUTHENTICATION FLOW

```
┌──────────┐                                          ┌──────────┐
│  Client  │                                          │  Server  │
└─────┬────┘                                          └────┬─────┘
      │                                                    │
      │  1. POST /api/auth/signup                         │
      │     { email, password, fullName, role }           │
      ├──────────────────────────────────────────────────>│
      │                                                    │
      │                                   2. Hash password │
      │                              (bcrypt, 10 rounds)   │
      │                                                    │
      │                                3. Save to MongoDB  │
      │                                                    │
      │                                4. Generate JWT     │
      │                                   (7-day expiry)   │
      │                                                    │
      │  5. Return { token, user }                        │
      │<──────────────────────────────────────────────────┤
      │                                                    │
      │  6. Store token in localStorage                   │
      │     localStorage.setItem('token', token)          │
      │                                                    │
      │                                                    │
      │  7. Subsequent requests:                          │
      │     Authorization: Bearer <token>                 │
      ├──────────────────────────────────────────────────>│
      │                                                    │
      │                              8. Verify JWT         │
      │                                 (middleware)       │
      │                                                    │
      │  9. Return protected data                         │
      │<──────────────────────────────────────────────────┤
      │                                                    │
```

**Key Components:**

- **Client:** React AuthContext + localStorage
- **Server:** JWT sign/verify, bcrypt hash/compare
- **Middleware:** requireAuth checks token on every protected route

---

## 🔔 NOTIFICATION SYSTEM FLOW

```
                    ┌─────────────────────┐
                    │   Cron Job          │
                    │   (Every 5 min)     │
                    └──────────┬──────────┘
                               │
                    1. Query appointments/routines
                       where date is today/tomorrow
                       and reminderSent = false
                               │
                    ┌──────────▼──────────┐
                    │  Filter candidates  │
                    │  (0-60 min away)    │
                    └──────────┬──────────┘
                               │
            ┌──────────────────┼──────────────────┐
            │                  │                  │
    ┌───────▼────────┐ ┌──────▼──────┐ ┌────────▼────────┐
    │   In-App       │ │   Email     │ │      SMS        │
    │ Notification   │ │ (Nodemailer)│ │    (Twilio)     │
    └───────┬────────┘ └──────┬──────┘ └────────┬────────┘
            │                  │                  │
    Save to MongoDB    SMTP Server       Phone Number
            │                  │           (E.164 format)
            │                  │                  │
    ┌───────▼────────┐ ┌──────▼──────┐ ┌────────▼────────┐
    │  Socket.IO     │ │   Gmail     │ │   Twilio API    │
    │  Broadcast     │ │   Server    │ │                 │
    └───────┬────────┘ └──────┬──────┘ └────────┬────────┘
            │                  │                  │
            │                  │                  │
    ┌───────▼────────┐ ┌──────▼──────┐ ┌────────▼────────┐
    │  User Browser  │ │ User Inbox  │ │   User Phone    │
    │  (Real-time)   │ │             │ │                 │
    └────────────────┘ └─────────────┘ └─────────────────┘
                               │
                    ┌──────────▼──────────┐
                    │  Update Database:   │
                    │  reminderSent=true  │
                    │externalReminderSent │
                    │      =true          │
                    └─────────────────────┘
```

**Timing:**

- Cron runs every 5 minutes
- Checks 60-minute window
- Prevents duplicates with flags

**Channels:**

1. **In-App:** Immediate via Socket.IO
2. **Email:** SMTP via Nodemailer
3. **SMS:** Twilio API with E.164 phone

---

## 💬 REAL-TIME CHAT FLOW (Socket.IO)

```
┌────────────┐                                      ┌────────────┐
│  User A    │                                      │  User B    │
│  Browser   │                                      │  Browser   │
└─────┬──────┘                                      └─────┬──────┘
      │                                                   │
      │  1. Connect to Socket.IO                         │
      ├─────────────────────┐                            │
      │                     │                            │
      │                ┌────▼─────┐                      │
      │                │ Socket.IO│                      │
      │                │  Server  │◄─────────────────────┤
      │                └────┬─────┘                      │
      │  2. Emit 'join-group'                            │
      │     { groupId: 'abc123' }                        │
      ├────────────────────>│                            │
      │                     │                            │
      │              3. socket.join('group:abc123')     │
      │                     │                            │
      │                     │◄────────────────────────────┤
      │                     │  4. Emit 'join-group'      │
      │                     │     { groupId: 'abc123' }  │
      │                     │                            │
      │              5. socket.join('group:abc123')     │
      │                     │                            │
      │                                                   │
      │  6. User A sends message                         │
      │     { groupId, message: 'Hello' }                │
      ├────────────────────>│                            │
      │                     │                            │
      │              7. Save to MongoDB                  │
      │                     │                            │
      │              8. Broadcast to room                │
      │         io.to('group:abc123').emit('message')   │
      │                     │                            │
      │  9. Receive message │                            │
      │◄────────────────────┤────────────────────────────>│
      │                     │         10. Receive message│
      │                                                   │
      │  11. Update UI      │              12. Update UI │
      │                                                   │
```

**Key Concepts:**

- **Rooms:** Namespaced channels (group:abc123)
- **Broadcasting:** Send to all in room
- **Persistent:** Messages saved to MongoDB
- **Real-time:** No polling, instant delivery

---

## 📤 FILE UPLOAD FLOW

```
┌────────────┐                                    ┌────────────┐
│  Browser   │                                    │   Server   │
└─────┬──────┘                                    └─────┬──────┘
      │                                                 │
      │  1. User selects file                          │
      │     <input type="file" />                      │
      │                                                 │
      │  2. Create FormData                            │
      │     formData.append('attachment', file)        │
      │     formData.append('description', text)       │
      │                                                 │
      │  3. POST /api/medical-records                  │
      │     Content-Type: multipart/form-data          │
      │     Authorization: Bearer <token>              │
      ├────────────────────────────────────────────────>│
      │                                                 │
      │                               4. Verify JWT     │
      │                                  (requireAuth)  │
      │                                                 │
      │                               5. Multer parses  │
      │                                  multipart data │
      │                                                 │
      │                               6. Validate:      │
      │                                  - File type    │
      │                                  - File size    │
      │                                                 │
      │                               7. Save file:     │
      │                                  uploads/12345- │
      │                                  timestamp.pdf  │
      │                                                 │
      │                               8. Save metadata: │
      │                                  MongoDB record │
      │                                  with file path │
      │                                                 │
      │  9. Return success                             │
      │     { _id, attachmentUrl, ... }                │
      │◄────────────────────────────────────────────────┤
      │                                                 │
      │                                                 │
      │  10. Later: View file                          │
      │      GET /api/medical-records/file/12345.pdf   │
      │      Authorization: Bearer <token>             │
      ├────────────────────────────────────────────────>│
      │                                                 │
      │                              11. Verify JWT     │
      │                                                 │
      │                              12. Check owner:   │
      │                                  userId matches │
      │                                                 │
      │  13. Stream file                               │
      │      res.sendFile(filepath)                    │
      │◄────────────────────────────────────────────────┤
      │                                                 │
```

**Security Layers:**

1. JWT authentication
2. File type validation
3. Size limits (10MB)
4. Ownership verification
5. No direct file access

---

## 🗃️ DATABASE SCHEMA RELATIONSHIPS

```
┌─────────────┐
│    User     │
│─────────────│
│ _id         │◄──────────┐
│ email       │           │
│ passwordHash│           │
│ fullName    │           │
│ role        │           │
└─────────────┘           │
                          │ userId (1:1)
                          │
                    ┌─────┴──────┐
                    │  Profile   │
                    │────────────│
                    │ userId     │
                    │ full_name  │
                    │ phone      │
                    │ address    │
                    │ emergency  │
                    │ conditions │
                    └────────────┘

┌─────────────┐                      ┌──────────────┐
│    User     │                      │ Appointment  │
│─────────────│                      │──────────────│
│ _id         │◄─────────────────────│ userId       │
└─────────────┘   userId (1:N)      │ title        │
                                     │ date         │
                                     │ time         │
                                     │ reminderSent │
                                     └──────────────┘

┌─────────────┐                      ┌──────────────┐
│    User     │                      │   Routine    │
│─────────────│                      │──────────────│
│ _id         │◄─────────────────────│ userId       │
└─────────────┘   userId (1:N)      │ title        │
                                     │ category     │
                                     │ time         │
                                     │ reminderSent │
                                     └──────────────┘

┌─────────────┐                      ┌──────────────┐
│    User     │                      │   Symptom    │
│─────────────│                      │──────────────│
│ _id         │◄─────────────────────│ userId       │
└─────────────┘   userId (1:N)      │ type         │
                                     │ severity     │
                                     │ date         │
                                     └──────────────┘

┌─────────────┐                      ┌──────────────────┐
│    User     │                      │ MedicalRecord    │
│─────────────│                      │──────────────────│
│ _id         │◄─────────────────────│ userId           │
└─────────────┘   userId (1:N)      │ type             │
                                     │ description      │
                                     │ attachmentUrl    │
                                     └──────────────────┘

┌─────────────┐                      ┌──────────────────┐
│    User     │                      │ Notification     │
│─────────────│                      │──────────────────│
│ _id         │◄─────────────────────│ userId           │
└─────────────┘   userId (1:N)      │ type             │
                                     │ message          │
                                     │ read             │
                                     └──────────────────┘

┌─────────────┐        createdBy     ┌──────────────────┐
│    User     │        (1:N)         │ SupportGroup     │
│─────────────│◄─────────────────────│──────────────────│
│ _id         │                      │ createdBy        │
└─────────────┘                      │ name             │
      ▲                              │ description      │
      │                              │ members[]        │
      │                              └──────────────────┘
      │ userId in members[]
      │ (M:N via array)
      └──────────────────────────────┘

┌─────────────┐                      ┌──────────────────┐
│    User     │      userId          │   GroupChat      │
│─────────────│◄─────────────────────│──────────────────│
│ _id         │      (1:N)           │ groupId          │
└─────────────┘                      │ userId           │
                                     │ message          │
                                     └──────────────────┘
                                            ▲
┌──────────────────┐                       │
│ SupportGroup     │       groupId         │
│──────────────────│       (1:N)           │
│ _id              │───────────────────────┘
│ name             │
└──────────────────┘
```

**Key Patterns:**

- **1:1** User ↔ Profile
- **1:N** User ↔ Appointments, Routines, Symptoms, Records, Notifications
- **M:N** User ↔ SupportGroups (via members array)
- **All indexed** on userId for fast queries

---

## 🎯 REQUEST/RESPONSE FLOW

### Typical Protected API Request

```
1. Frontend Component
   └─> Call apiFetch('/api/appointments')
        │
        ├─> Get token from localStorage
        │
        ├─> Set Authorization: Bearer <token>
        │
        └─> fetch(url, { headers })

2. Vite Dev Proxy
   └─> Forward to http://localhost:4000

3. Express Server
   └─> CORS middleware
        │
        ├─> morgan (logging)
        │
        └─> Route: GET /api/appointments

4. Auth Middleware
   └─> Extract token from header
        │
        ├─> Verify JWT signature
        │
        ├─> Check expiration
        │
        ├─> Set req.userId
        │
        └─> next()

5. Route Handler
   └─> Query MongoDB
        │
        ├─> AppointmentModel.find({ userId: req.userId })
        │
        └─> Return results

6. Response
   └─> JSON: [{ _id, title, date, ... }]

7. Frontend
   └─> Update component state
        │
        └─> Re-render with data
```

---

## 🔄 DATA FLOW DIAGRAMS

### Appointment with Reminder

```
User Action                 Database                Cron Job               External Services
─────────────────────────────────────────────────────────────────────────────────────────
    │                          │                        │                         │
    │ 1. Create appointment    │                        │                         │
    ├─────────────────────────>│                        │                         │
    │                          │                        │                         │
    │                     Save with:                    │                         │
    │                     reminderSent=false            │                         │
    │                          │                        │                         │
    │                          │                        │                         │
    │                          │◄───────────────────────┤                         │
    │                          │   2. Every 5 min:      │                         │
    │                          │      Query upcoming    │                         │
    │                          │                        │                         │
    │                          │   3. Found appointment │                         │
    │                          │      (55 min away)     │                         │
    │                          │                        │                         │
    │                          │                   4. Create                      │
    │                          │◄──────────────────notification                  │
    │                          │                        │                         │
    │                          │   5. Get profile       │                         │
    │                          │──────────────────────> │                         │
    │                          │                        │                         │
    │                          │                        ├────────────────────────>│
    │                          │                        │   6. Send email         │
    │                          │                        │                         │
    │                          │                        ├────────────────────────>│
    │                          │                        │   7. Send SMS           │
    │                          │                        │                         │
    │                          │◄───────────────────────┤                         │
    │                          │   8. Update:           │                         │
    │                          │      reminderSent=true │                         │
    │                          │                        │                         │
    │◄─────────────────────────┤                        │                         │
    │   9. Socket.IO emit      │                        │                         │
    │      (real-time notify)  │                        │                         │
    │                          │                        │                         │
```

---

## 🏗️ COMPONENT HIERARCHY

### Dashboard Page Structure

```
Dashboard (Page)
│
├─ UpcomingReminders (Widget)
│  ├─ ReminderItem
│  ├─ ReminderItem
│  └─ ReminderItem
│
├─ AppointmentList (Widget)
│  ├─ AppointmentCard
│  ├─ AppointmentCard
│  └─ AddButton
│
├─ MedicationList (Widget)
│  ├─ MedicationCard
│  ├─ MedicationCard
│  └─ AddButton
│
├─ EmergencyContacts (Widget)
│  ├─ ContactCard
│  ├─ ContactCard
│  └─ ContactCard
│
├─ DailyRoutineBuilder (Widget)
│  ├─ TaskList
│  │  ├─ TaskItem
│  │  ├─ TaskItem
│  │  └─ TaskItem
│  └─ TaskForm
│
└─ SymptomTracker (Widget)
   ├─ SymptomList
   │  ├─ SymptomEntry
   │  └─ SymptomEntry
   └─ SymptomForm
```

### Support Groups Page Structure

```
SupportGroups (Page)
│
├─ Header
│  ├─ Title
│  └─ CreateGroupButton
│
├─ GroupGrid
│  ├─ GroupCard
│  │  ├─ GroupInfo
│  │  └─ JoinButton
│  ├─ GroupCard
│  └─ GroupCard
│
├─ CreateGroupModal (Conditional)
│  └─ GroupForm
│
└─ ChatModal (Conditional)
   ├─ ChatBot
   │  ├─ MessageList
   │  │  ├─ BotMessage
   │  │  ├─ UserMessage
   │  │  └─ BotMessage
   │  └─ InputForm
   │
   └─ GroupChat (After joining)
      ├─ MessageList
      │  ├─ Message
      │  ├─ Message
      │  └─ Message
      └─ InputForm
```

---

## 🌐 DEPLOYMENT ARCHITECTURE

### Development

```
┌────────────────────────────────────────┐
│        Developer Machine                │
│                                        │
│  ┌──────────────┐  ┌──────────────┐  │
│  │  Vite Dev    │  │   tsx watch  │  │
│  │  Server      │  │   (Backend)  │  │
│  │  :5173       │  │   :4000      │  │
│  └──────────────┘  └──────────────┘  │
│         ↑                  ↑          │
│         └──────Proxy───────┘          │
│                                        │
│  ┌──────────────────────────────────┐ │
│  │   MongoDB (Local/Atlas)          │ │
│  └──────────────────────────────────┘ │
│                                        │
└────────────────────────────────────────┘
```

### Production (Recommended)

```
┌────────────────────────────────────────────────────┐
│                  CDN (CloudFront)                   │
│              Static Assets (dist/)                  │
└─────────────────────┬──────────────────────────────┘
                      │
┌─────────────────────▼──────────────────────────────┐
│              Load Balancer (ALB)                    │
└─────────────────────┬──────────────────────────────┘
                      │
        ┌─────────────┴─────────────┐
        │                           │
┌───────▼────────┐        ┌─────────▼────────┐
│   EC2/Heroku   │        │   EC2/Heroku     │
│   Instance 1   │        │   Instance 2     │
│                │        │                  │
│ ┌────────────┐ │        │ ┌────────────┐  │
│ │ Express +  │ │        │ │ Express +  │  │
│ │ Socket.IO  │ │        │ │ Socket.IO  │  │
│ └────────────┘ │        │ └────────────┘  │
└────────┬───────┘        └────────┬─────────┘
         │                         │
         └──────────┬──────────────┘
                    │
        ┌───────────▼───────────┐
        │  MongoDB Atlas        │
        │  (Replica Set)        │
        └───────────────────────┘
                    │
        ┌───────────▼───────────┐
        │  S3 (File Storage)    │
        └───────────────────────┘
                    │
        ┌───────────┼────────────┐
        │           │            │
   ┌────▼────┐ ┌───▼────┐ ┌─────▼─────┐
   │ Twilio  │ │Nodemailer│ │  Redis   │
   │  (SMS)  │ │ (Email) │ │  (Cache) │
   └─────────┘ └─────────┘ └───────────┘
```

---

## 📊 STATE MANAGEMENT FLOW

```
┌─────────────────────────────────────────────┐
│           Application State                  │
├─────────────────────────────────────────────┤
│                                             │
│  ┌────────────────┐                         │
│  │  AuthContext   │ (Global)                │
│  │  ─────────────  │                         │
│  │  user: User    │                         │
│  │  loading: bool │                         │
│  │  refresh()     │                         │
│  └────────┬───────┘                         │
│           │                                  │
│           └─────────────────┐               │
│                             │               │
│  ┌──────────────┐    ┌──────▼──────┐       │
│  │ localStorage │    │  Component  │       │
│  │ ────────────  │    │   State     │       │
│  │ - token      │    │  ────────────│       │
│  │ - forumPosts │    │  - loading  │       │
│  │ - completed  │    │  - error    │       │
│  │   Videos     │    │  - data[]   │       │
│  └──────────────┘    └─────────────┘       │
│                                             │
└─────────────────────────────────────────────┘

Flow:
1. Page loads → Check AuthContext
2. If no user → Redirect to /auth
3. If user → Fetch data (API)
4. Update component state
5. Render UI
6. User action → Update state → Re-render
```

---

## 🔒 SECURITY LAYERS

```
Layer 1: Frontend Protection
┌─────────────────────────────┐
│ - ProtectedRoute wrapper    │
│ - Token in localStorage     │
│ - Input validation          │
│ - XSS prevention (React)    │
└───────────┬─────────────────┘
            │
Layer 2: Network Security
┌───────────▼─────────────────┐
│ - HTTPS/TLS                 │
│ - CORS configuration        │
│ - Rate limiting             │
└───────────┬─────────────────┘
            │
Layer 3: Application Security
┌───────────▼─────────────────┐
│ - JWT verification          │
│ - Auth middleware           │
│ - Password hashing (bcrypt) │
│ - File type validation      │
└───────────┬─────────────────┘
            │
Layer 4: Data Security
┌───────────▼─────────────────┐
│ - User-specific queries     │
│ - Ownership verification    │
│ - MongoDB injection prevent │
│ - Encrypted connections     │
└─────────────────────────────┘
```

---

## 🎨 UI/UX FLOW

### User Journey: New User to First Appointment

```
Step 1: Landing Page
   │
   ├─> Click "Get Started"
   │
Step 2: Auth Page
   │
   ├─> Fill signup form
   │
   ├─> Submit
   │
   └─> Redirect to Dashboard
       │
Step 3: Dashboard (Empty State)
   │
   ├─> See "No appointments yet" message
   │
   ├─> Click "Add Appointment"
   │
Step 4: Appointment Form
   │
   ├─> Fill: Title, Date, Time, Location
   │
   ├─> Optional: Phone for SMS
   │
   ├─> Submit
   │
   └─> Success message
       │
Step 5: Dashboard (With Appointment)
   │
   ├─> See appointment card
   │
   └─> See upcoming reminder widget
       │
Step 6: Wait for Reminder
   │
   └─> Cron job triggers notification
       │
       ├─> Browser notification (Socket.IO)
       │
       ├─> Email notification
       │
       └─> SMS notification
```

---

## 📱 RESPONSIVE DESIGN BREAKPOINTS

```
Mobile (< 640px)           Tablet (640-1024px)      Desktop (> 1024px)
─────────────────────────────────────────────────────────────────────
┌─────────────┐           ┌───────────────────┐    ┌─────────────────────────┐
│   Stack     │           │    2 Columns      │    │      3-4 Columns        │
│  Vertical   │           │                   │    │                         │
│             │           │  ┌──────┬──────┐  │    │  ┌─────┬─────┬─────┐   │
│  ┌──────┐   │           │  │Widget│Widget│  │    │  │Wdgt │Wdgt │Wdgt │   │
│  │Widget│   │           │  └──────┴──────┘  │    │  └─────┴─────┴─────┘   │
│  └──────┘   │           │  ┌──────┬──────┐  │    │  ┌─────┬─────┬─────┐   │
│  ┌──────┐   │           │  │Widget│Widget│  │    │  │Wdgt │Wdgt │Wdgt │   │
│  │Widget│   │           │  └──────┴──────┘  │    │  └─────┴─────┴─────┘   │
│  └──────┘   │           │                   │    │                         │
│  ┌──────┐   │           └───────────────────┘    └─────────────────────────┘
│  │Widget│   │
│  └──────┘   │
└─────────────┘

Tailwind Classes:         Tailwind Classes:        Tailwind Classes:
- w-full                  - md:grid-cols-2         - lg:grid-cols-3
- grid-cols-1             - md:flex-row            - lg:grid-cols-4
- flex-col                                         - lg:flex-row
```

---

**This visual documentation provides a comprehensive architectural overview of the entire system. Use these diagrams to explain system design and data flow during your interview!**

